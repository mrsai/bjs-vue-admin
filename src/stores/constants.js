/**
 * 常量以及一些全局使用的映射关系和字符串
 * 以及一些需要写死的key的值和名称的映射关系
 */

/**
 * 主题
 */
export const Theme = {
  Dark: "dark",
  Light: "light",
}

/**
 * 唯一性保证，一般增加在字符串前面，保证其变量名称的唯一性
 * 可能还是用不着
 */
export const Profix = "_bjs_";